type Mutation {
  novoUsuario (
    # nome: String,
    # email: String,
    # idade: Int
    dados: UsuarioInput!
  ): Usuario!

  excluirUsuario(id: Int): Usuario

  alterarUsuario(
    id: Int
    nome: String
    email: String
    idade: Int
  ): Usuario!
}